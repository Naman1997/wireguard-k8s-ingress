apiVersion: v1
kind: Service
metadata:
  name: wireguard-lb-service
spec:
  selector:
    k8s-app: wireguard-lb
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
    - name: https
      protocol: TCP
      port: 443
      targetPort: 443
